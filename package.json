{
  "name": "git-guardrails",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "bun build --compile --outfile dist/git src/index.ts",
    "test": "bun test",
    "install-guardrails": "bash scripts/install.sh",
    "release": "bash scripts/release.sh"
  },
  "devDependencies": {
    "bun-types": "^1.3.10"
  }
}
